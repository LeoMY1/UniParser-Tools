# UniParser MCP：trigger / get-result 默认参数。
# 服务根 URL 与 API Key 由进程环境提供（建议在 Cursor ``.cursor/mcp.json`` 的 ``env`` 中设置
# ``UNIPARSER_BASE_URL``、``UNIPARSER_API_KEY``）。

default_trigger_url:
  lang: unknown
  sync: true
  textual: 2
  table: 2
  molecule: 1
  chart: 1
  figure: 0
  expression: 1
  equation: 2

default_trigger_file:
  lang: unknown
  sync: true
  textual: 2
  table: 2
  molecule: 1
  chart: 1
  figure: 0
  expression: 1
  equation: 2

default_get_result:
  textual: markdown
  table: markdown
  molecule: markdown
  chart: markdown
  figure: markdown
  expression: markdown
  equation: markdown
  content: true
  objects: false
  pages_dict: false
  pages_tree: false
  molecule_source: true
